<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>

<body>
    <h1>Bienvenido a mi correo electrónico</h1>
    <p>Gracias por abrir mi correo electrónico. Para activar la llamada a mi endpoint JavaScript, haga clic en el botón
        de abajo:</p>
    <button onclick="callEndpoint()">Activar</button>
    <button onclick="cerrar()">Declinar</button>
    <script>
        var openedWindow;
        async function callEndpoint() {

            let params = new URLSearchParams(location.search);
            let id = params.get('id');
            axios.post(`http://localhost:3000/renovar/${id}`, {
            })
                .then(function (response) {
                    console.log(response);
                })
                .catch(function (error) {
                    console.log(error);
                })
        }

        function cerrar() {
            console.log("cerrar");
            window.location.href = "http://www.gmail.com";
        }

    </script>

</body>

</html>